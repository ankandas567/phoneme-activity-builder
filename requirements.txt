# Phoneme Activity Builder — install requirements
# This is a Node.js / Next.js project (not Python).
# Install Node.js first, then run: npm install

# Runtime
nodejs>=18.0.0
npm>=9.0.0

# Main packages (installed via package.json with npm install)
next==16.3.0
react==19.2.8
react-dom==19.2.8

# Dev / build tools
tailwindcss>=4
@tailwindcss/postcss>=4
eslint>=9
eslint-config-next==16.3.0
